os2iotFrontend:
  image:
    registry: ghcr.io/itk-dev
    repository: os2iot-frontend
    tag: 'latest'
    pullPolicy: Always

  replicaCount: 1

  service:
    port: 8081

  containerPort: 8081

  ingress:
    host: frontend.os2iot.dk

  resources:
    limits:
      cpu: 150m
      memory: 512Mi
    requests:
      cpu: 5m
      memory: 128Mi

  config:
    production: "true"
    baseUrl: "https://backend.os2iot.dk/api/v1/"
    tablePageSize: "25"